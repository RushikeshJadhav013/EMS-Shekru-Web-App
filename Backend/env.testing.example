# Copy to .env.testing for local/test runs
ENVIRONMENT=testing
TESTING_OTP=123456
ENABLE_EMAIL_OTP=false

# Database (update if needed)
DATABASE_URL=mysql+pymysql://staffly:staff9612@localhost/empl

# JWT
JWT_SECRET=supersecretjwtkey
JWT_ALGORITHM=HS256

# SMTP (leave blank when ENABLE_EMAIL_OTP=false)
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=
